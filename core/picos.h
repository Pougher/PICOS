#ifndef PICOS_H
#define PICOS_H

#include "build_type.h"
#include "graphics.h"
#include "renderer.h"
#include "interrupt.h"

extern struct InterruptHandler* interrupt_handler;
extern struct renderer* renderer;

#endif
